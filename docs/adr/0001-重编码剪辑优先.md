# 默认重编码精确剪辑，流拷贝仅作草稿模式

剪辑默认走 `libx264` 重编码（`-ss` 前置帧级精确 + `-crf 18~20` + `-movflags +faststart`），因为实测素材关键帧间隔高达 4~7 秒（p95 3~5.5s），`-c copy` 流拷贝的剪切点会吸附关键帧，误差最高 ±3.5~7 秒，无法满足精确剪辑需求。流拷贝以 `--copy` 开关保留为草稿/快速预览模式，误差为已知约束。