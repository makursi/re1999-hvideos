# re1999-hvideos 上下文集

把游戏《重返未来：1999》官方短片素材批量剪辑成新视频、以及从视频提取截图帧的工具项目。素材位于 `media/raw`（只读，永不修改），剪辑产物输出到 `media/exports`，截图产物输出到 `media/screenshots`。

## Language

**剪辑清单（manifest）**:
描述**一集**导出内容的唯一事实来源（JSON 文件）。按集分文件存放，跟随该集剪辑产物目录（`media/exports/epN/manifest.json`）。CLI 以集为最小处理单位，读取该集的 manifest 驱动批处理导出。
_Avoid_: 剪辑文件、任务列表、config

**片段**:
一个源视频 + 一个时间范围 `[in, out]`。当前阶段一个剪辑产物恰好是一个片段（一对一裁剪）；多片段拼接（合辑）是后续迭代的能力，不改变"片段"这一定义。
_Avoid_: 剪辑段、素材段、segment 条目

**截图**:
从源视频的**单个时间点**提取的一帧图像。用时间戳 `at` 描述（而非范围 `in/out`），格式为 jpg/png/webp，产物落在 `media/screenshots`。与"片段"（时间范围）是不同概念。
_Avoid_: 抓帧、定格帧、截图帧

**纯色帧（solid frame）**:
整帧像素几乎同一亮度的无内容帧（黑场 ≈ 0、白场/频闪 ≈ 255），由亮度均值 YAVG 处于极端区间**且**标准差 YSTD 低于阈值判定。静态内容帧（静帧/定格镜头）不是纯色帧。
_Avoid_: 坏帧、无内容帧、黑屏帧

**有效帧**:
非纯色帧，即画面有实际内容的帧；静态（不动）但有内容的帧也算有效帧。

**纠偏（auto-shift）**:
截图执行时发现 `at` 落在纯色帧上，自动**向后**逐帧搜索最近有效帧并输出的机制（窗口上限 64 帧，出窗无有效帧则按严格模式单条报错）。日志报告实际取帧时刻与偏移量。
_Avoid_: 偏移、补帧、校正

**截图规格（frames spec）**:
描述**一集**截图导出内容的唯一事实来源（JSON 文件）。跟随该集截图产物目录（`media/screenshots/epN/frames.json`），是该集截图处理的唯一输入。
_Avoid_: 截图清单、截图任务

**导出产物**:
一个片段或一条截图被处理后写出的最终文件，落在 `media/exports/epN`（剪辑产物，视频）或 `media/screenshots/epN`（截图产物，图像）。当前阶段剪辑产物与片段一一对应，截图产物与其来源时间点一一对应。
_Avoid_: 输出、成品、渲染结果

## Rules

- **素材内容只读**：`media/raw` 下的源文件内容永不修改（不转码、不裁剪）、永不删除；但文件名允许一次性规范化为 ASCII（`epNN.mp4`），中文标题映射保存在 `media/raw/videos/README.md`。
- **路径全 ASCII**：所有参与流水线的文件路径（输入、输出、临时文件）只含 `[A-Za-z0-9._-]`，防止中文乱码。
- **产物与规格同目录**：剪辑清单跟随剪辑产物（`media/exports/epN/manifest.json`）、截图规格跟随截图产物（`media/screenshots/epN/frames.json`）。规格文件进 git（.gitignore 例外保留），大批量媒体产物不提交。
- **目录映射显式优先**：规格条目可显式指定产物目录；缺省时产物落在规格文件自身所在目录。
- **渐进演进**：先做一对一裁剪，再扩展多片段拼接；schema 演进时保持"片段"与"截图"的语义不变。
- **防坏帧自动纠偏**：截图规格中的 `at` 是**意图时刻**；执行时若该帧被判为纯色帧，自动向后纠偏到最近有效帧（默认开启，`--strict` 关闭——严格模式判坏即单条报错，不输出）。规则"截图产物与其规格 `at` 对应"软化为：产物记住对应 `at`，实际取帧时刻可能略晚并记录在日志中；`at` 不变，规格不回写。