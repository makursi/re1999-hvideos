# re1999-hvideos 上下文集

把《重返未来：1999》官方短片等**任意素材项目**批量剪辑成片段、以及从视频提取截图帧的通用媒体工具项目。媒体目录采用"输入 → 操作 → 输出"三段式布局（ADR-0009）：原始素材位于 `media/input/<项目>`（只读、永不修改、不入 git），规格位于 `media/work/<项目>`（版本化），产物默认落在 `media/output/<项目>`（不入 git）。

## Language

**项目（project）**:
三段布局的顶级分组目录（`media/input/<项目>` / `media/work/<项目>` / `media/output/<项目>` 三者同名同构）。一个项目 = 一组可独立处理的素材与其规格。CLI 以 `--project <name>` 过滤（缺省 = 扫描全部项目）。
_Avoid_: 素材组、来源、series

**单元（unit）**:
项目内的最小处理单位，以 `<单元名>` 目录组织规格与产物。单元名是各项目自己的数据（如 1999 项目的 `ep1`~`ep7`）。
_Avoid_: 集、episode、epN（目录名等数据里出现不算违规，术语里不用）

**剪辑清单（manifest）**:
描述**一个单元**导出内容的唯一事实来源（JSON 文件）。按项目/类/单元分目录存放（`media/work/<项目>/clips/<单元>/manifest.json`）。CLI 以项目+单元为最小处理单位，读取该单元的 manifest 驱动批处理导出。
_Avoid_: 剪辑文件、任务列表、config

**片段**:
一个源视频 + 一个时间范围 `[in, out]`。当前阶段一个剪辑产物恰好是一个片段（一对一裁剪）；多片段拼接（合辑）是后续迭代的能力，不改变"片段"这一定义。
_Avoid_: 剪辑段、素材段、segment 条目

**截图**:
从源视频的**单个时间点**提取的一帧图像。用时间戳 `at` 描述（而非范围 `in/out`），格式为 jpg/png/webp，产物默认落在 `media/output/<项目>/screenshots/<单元>/`。与"片段"（时间范围）是不同概念。
_Avoid_: 抓帧、定格帧、截图帧

**纯色帧（solid frame）**:
整帧像素几乎同一亮度的无内容帧（黑场 ≈ 0、白场/频闪 ≈ 255），由亮度均值 YAVG 处于极端区间**且**标准差 YSTD 低于阈值判定。静态内容帧（静帧/定格镜头）不是纯色帧。
_Avoid_: 坏帧、无内容帧、黑屏帧

**有效帧**:
非纯色帧，即画面有实际内容的帧；静态（不动）但有内容的帧也算有效帧。

**纠偏（auto-shift）**:
截图执行时发现 `at` 落在纯色帧上，自动**向后**逐帧搜索最近有效帧并输出的机制（窗口上限 64 帧，出窗无有效帧则按严格模式单条报错）。日志报告实际取帧时刻与偏移量。
_Avoid_: 偏移、补帧、校正

**截图规格（frames spec）**:
描述**一个单元**截图导出内容的唯一事实来源（JSON 文件）。位于 `media/work/<项目>/screenshots/<单元>/frames.json`，是该单元截图处理的唯一输入。
_Avoid_: 截图清单、截图任务

**导出产物**:
一个片段或一条截图被处理后写出的最终文件，默认落在 `media/output/<项目>/clips/<单元>`（剪辑产物，视频）或 `media/output/<项目>/screenshots/<单元>`（截图产物，图像）。当前阶段剪辑产物与片段一一对应、截图产物与其来源时间点一一对应。
_Avoid_: 输出、成品、渲染结果

**输入/操作/输出目录（input/work/output）**:
三段式布局的三层：`media/input` 存放只读原始素材（按项目分、不入 git）；`media/work` 存放规格（版本化、按 项目/类(clips|screenshots)/单元 分）；`media/output` 存放产物（不入 git，默认路径 = work 中对应规格目录的"work→output 镜像"）。`media/temp` 是临时工作区，不属于这三层任何一层。
_Avoid_: 素材目录、产物目录、临时目录（temp 不是输出层）

**配置项（config knob）**:
机器/部署相关的可变值,由环境变量提供、带文档化默认值(规格扫描根、产物默认根、临时目录、工具二进制),统称"配置面";优先级为 CLI 显式参数 > 环境变量(shell 优先于 `.env` 文件)> 默认值。与领域常量(帧率 25、纠偏窗口上限、纯色帧判定阈值、ASCII 路径规则)严格区分——领域常量是领域真值,永远不是配置。素材 `source` 路径同样不是配置:它是规格数据的一部分,尊重素材只读规则。
_Avoid_: 环境变量参数、options、settings

## Rules

- **素材内容只读**：`media/input/<项目>` 下的源文件内容永不修改（不转码、不裁剪）、永不删除；但文件名允许一次性规范化为 ASCII（如 1999 项目的 `epNN.mp4`），中文标题映射保存在 `media/input/<项目>/README.md`。
- **路径全 ASCII**：所有参与流水线的文件路径（输入、输出、临时文件）只含 `[A-Za-z0-9._-]`，防止中文乱码。
- **规格与产物分层**：规格归操作目录（`media/work/<项目>/<类>/<单元>/…json`，进 git），产物默认归输出目录镜像（`media/output/<项目>/<类>/<单元>/`，不提交）。
- **目录映射显式优先**：规格条目可显式指定产物目录（snap 的 `dir`、clip 的 `-o`）；缺省时产物落在"work→output 镜像"目录。
- **渐进演进**：先做一对一裁剪，再扩展多片段拼接；schema 演进时保持"片段"与"截图"的语义不变。
- **防坏帧自动纠偏**：截图规格中的 `at` 是**意图时刻**；执行时若该帧被判为纯色帧，自动向后纠偏到最近有效帧（默认开启，`--strict` 关闭——严格模式判坏即单条报错，不输出）。规则"截图产物与其规格 `at` 对应"软化为：产物记住对应 `at`，实际取帧时刻可能略晚并记录在日志中；`at` 不变，规格不回写。