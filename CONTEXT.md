# re1999-hvideos 上下文集

把游戏《重返未来：1999》官方短片素材批量剪辑成新视频的工具项目。素材位于 `media/raw`（只读，永不修改），产物输出到 `media/exports`。

## Language

**剪辑清单（manifest）**:
描述一次导出内容的唯一事实来源（JSON 文件），CLI 的唯一输入，驱动批处理导出。
_Avoid_: 剪辑文件、任务列表、config

**片段**:
一个源视频 + 一个时间范围 `[in, out]`。当前阶段一个导出产物恰好是一个片段（一对一裁剪）；多片段拼接（合辑）是后续迭代的能力，不改变"片段"这一定义。
_Avoid_: 剪辑段、素材段、segment 条目

**导出产物**:
一个片段被裁剪后写出的最终视频文件，落在 `media/exports`。当前阶段与片段一一对应。
_Avoid_: 输出、成品、渲染结果

## Rules

- **素材内容只读**：`media/raw` 下的源文件内容永不修改（不转码、不裁剪）、永不删除；但文件名允许一次性规范化为 ASCII（`epNN.mp4`），中文标题映射保存在 `media/raw/videos/README.md`。
- **路径全 ASCII**：所有参与流水线的文件路径（输入、输出、临时文件）只含 `[A-Za-z0-9._-]`，防止中文乱码。
- **渐进演进**：先做一对一裁剪，再扩展多片段拼接；schema 演进时保持"片段"语义不变。